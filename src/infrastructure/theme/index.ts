import {space, lineHeights} from './spacing';
import {sizes, units} from './sizes';
import {fontWeights, fontSizes, fontFamily} from './fonts';
import {colors} from './colors';

export const theme = {
  colors,
  space,
  lineHeights,
  sizes,
  fontSizes,
  fontWeights,
  units,
  fontFamily,
};
